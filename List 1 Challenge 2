cars = []

addname = True

while addname == True:
  newcar = str(input("Enter a car brand or press x to exit"))
  
  if newcar == "x":
    print("Exiting...")
    addname = False
    print("In the list =", cars)
  
  else:
    cars.append(newcar)


